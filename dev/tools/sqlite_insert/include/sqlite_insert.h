#pragma once

#include <spdlog/spdlog.h>

#include <nlohmann/json.hpp>

#include <sqlite3.h>

class sqlite_insert
{
private:
    /* data */
public:
    sqlite_insert(/* args */);
    ~sqlite_insert();
};
